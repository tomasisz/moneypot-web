import React from 'react'


const SectionDiv = ({children}) => (
    <div
        style={{
            margin: '1rem 2rem',
            padding: '1rem',
        }}
    >
        {children}
    </div>
)
export default SectionDiv
